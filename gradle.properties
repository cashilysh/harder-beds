# Mod Configuration
jarName=harderbeds
modVersion=1.0.3
loomVersion=1.17-SNAPSHOT
defaultTargetVersion=26.3

# Runtime dependencies (implementation)
deps.implementation=fabric-api

# Compile-only dependencies (API mods you code against but don't bundle)
deps.compileOnly=modmenu,cloth-config

deps.localRuntime=modmenu,cloth-config
